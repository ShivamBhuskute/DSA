#include <iostream>
#include <vector>
#include <climits>
#include <string>
#include <algorithm>
#include <cmath>
using namespace std;

bool f(int n, int k) {
    return (n & (1 << (k - 1))) != 0;
}

int main() {
    
return 0;
}